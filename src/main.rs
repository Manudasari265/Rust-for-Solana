mod levels {
    pub mod floor_1;
    pub mod floor_2;
    pub mod floor_3;
}

use levels::floor_1::numbers;
use levels::floor_2::booleans;
use levels::floor_3::strings;

fn main() {
    // numbers();
    // booleans();
    strings();
}